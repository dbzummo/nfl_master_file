# Example only — replace with real values in _secrets/.env (never commit/share).
ODDS_API_KEY=REPLACE_ME
MSF_API_KEY=REPLACE_ME
DATABASE_URL=postgres://USER:PASS@HOST:5432/DBNAME
JWT_SECRET=CHANGE_ME
